{
  "compilerOptions": {
    /* --- Configuración Básica --- */
    "target": "es2020",               // Versión moderna de JS compatible con Node 14+
    "module": "commonjs",             // Estándar para Node.js
    "moduleResolution": "node",       // Algoritmo de resolución de módulos clásico
    "rootDir": "./src",               // Dónde está tu código fuente
    "outDir": "./dist",               // Dónde se guardará el código compilado
    
    /* --- Interoperabilidad --- */
    "esModuleInterop": true,          // Permite importar módulos CommonJS (como express) con 'import' normal
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,        // Permite importar archivos .json

    /* --- Estrictez (Mejores Prácticas) --- */
    "strict": true,                   // Activa todas las verificaciones estrictas de tipos
    "noImplicitAny": true,            // No permite variables sin tipo definido
    "skipLibCheck": true              // Salta chequeo de tipos en librerías externas (ahorra tiempo)
  },
  "include": ["src/**/*"],            // Incluir todo lo que esté en src
  "exclude": ["node_modules", "dist"] // Ignorar dependencias y builds
}